
NAME: eddie_ros

STATES:
    S_START,
    S_CONFIGURE,
    S_IDLE,
    S_COMPILE,
    S_EXECUTE,
    S_MOVE_HOME,
    S_MOVE_PREGRASP,
    S_MOVE_GRASP,
    S_CLOSE_GRIPPER,
    S_MOVE_PREPLACE,
    S_MOVE_PLACE,
    S_OPEN_GRIPPER,
    S_RETURN_HOME,
    S_EXIT

START_STATE: @S_START
CURRENT_STATE: @S_START
END_STATE: @S_EXIT

EVENTS:
    E_CONFIGURE_ENTERED,
    E_CONFIGURE_EXIT,
    E_IDLE_ENTERED,
    E_IDLE_EXIT_EXECUTE,
    E_IDLE_EXIT_COMPILE,
    E_COMPILE_ENTERED,
    E_COMPILE_EXIT,
    E_EXECUTE_ENTERED,
    E_STEP,
    E_MOVE_HOME_DONE,
    E_MOVE_PREGRASP_DONE,
    E_MOVE_GRASP_DONE,
    E_CLOSE_GRIPPER_DONE,
    E_MOVE_PREPLACE_DONE,
    E_MOVE_PLACE_DONE,
    E_OPEN_GRIPPER_DONE,
    E_RETURN_HOME_DONE

TRANSITIONS:
    T_START_CONFIGURE:
        FROM: @S_START
        TO: @S_CONFIGURE
    T_CONFIGURE_IDLE:
        FROM: @S_CONFIGURE
        TO: @S_IDLE
    T_IDLE_IDLE:
        FROM: @S_IDLE
        TO: @S_IDLE
    T_IDLE_EXECUTE:
        FROM: @S_IDLE
        TO: @S_EXECUTE
    T_IDLE_COMPILE:
        FROM: @S_IDLE
        TO: @S_COMPILE
    T_COMPILE_EXECUTE:
        FROM: @S_COMPILE
        TO: @S_EXECUTE
    T_EXECUTE_MOVE_HOME:
        FROM: @S_EXECUTE
        TO: @S_MOVE_HOME
    T_MOVE_HOME_PREGRASP:
        FROM: @S_MOVE_HOME
        TO: @S_MOVE_PREGRASP
    T_MOVE_PREGRASP_GRASP:
        FROM: @S_MOVE_PREGRASP
        TO: @S_MOVE_GRASP
    T_MOVE_GRASP_CLOSE:
        FROM: @S_MOVE_GRASP
        TO: @S_CLOSE_GRIPPER
    T_CLOSE_PREPLACE:
        FROM: @S_CLOSE_GRIPPER
        TO: @S_MOVE_PREPLACE
    T_PREPLACE_PLACE:
        FROM: @S_MOVE_PREPLACE
        TO: @S_MOVE_PLACE
    T_PLACE_OPEN:
        FROM: @S_MOVE_PLACE
        TO: @S_OPEN_GRIPPER
    T_OPEN_RETURN:
        FROM: @S_OPEN_GRIPPER
        TO: @S_RETURN_HOME
    T_RETURN_EXIT:
        FROM: @S_RETURN_HOME
        TO: @S_EXIT

REACTIONS:
    R_E_CONFIGURE_EXIT:
        WHEN: @E_CONFIGURE_EXIT
        DO: @T_CONFIGURE_IDLE
        FIRES: @E_IDLE_ENTERED
    R_E_IDLE_EXIT_EXECUTE:
        WHEN: @E_IDLE_EXIT_EXECUTE
        DO: @T_IDLE_EXECUTE
        FIRES: @E_EXECUTE_ENTERED
    R_E_IDLE_EXIT_COMPILE:
        WHEN: @E_IDLE_EXIT_COMPILE
        DO: @T_IDLE_COMPILE
        FIRES: @E_COMPILE_ENTERED
    R_E_COMPILE_EXIT:
        WHEN: @E_COMPILE_EXIT
        DO: @T_COMPILE_EXECUTE
        FIRES: @E_EXECUTE_ENTERED
    R_E_STEP1:
        WHEN: @E_STEP
        DO: @T_START_CONFIGURE
        FIRES: @E_CONFIGURE_ENTERED
    R_E_STEP2:
        WHEN: @E_STEP
        DO: @T_IDLE_IDLE
        FIRES: @E_IDLE_ENTERED
    R_E_STEP3:
        WHEN: @E_STEP
        DO: @T_EXECUTE_MOVE_HOME
        FIRES: @E_MOVE_HOME_DONE
    R_MOVE_HOME_DONE:
        WHEN: @E_MOVE_HOME_DONE
        DO: @T_MOVE_HOME_PREGRASP
        FIRES: @E_MOVE_PREGRASP_DONE
    R_MOVE_PREGRASP_DONE:
        WHEN: @E_MOVE_PREGRASP_DONE
        DO: @T_MOVE_PREGRASP_GRASP
        FIRES: @E_MOVE_GRASP_DONE
    R_MOVE_GRASP_DONE:
        WHEN: @E_MOVE_GRASP_DONE
        DO: @T_MOVE_GRASP_CLOSE
        FIRES: @E_CLOSE_GRIPPER_DONE
    R_CLOSE_GRIPPER_DONE:
        WHEN: @E_CLOSE_GRIPPER_DONE
        DO: @T_CLOSE_PREPLACE
        FIRES: @E_MOVE_PREPLACE_DONE
    R_MOVE_PREPLACE_DONE:
        WHEN: @E_MOVE_PREPLACE_DONE
        DO: @T_PREPLACE_PLACE
        FIRES: @E_MOVE_PLACE_DONE
    R_MOVE_PLACE_DONE:
        WHEN: @E_MOVE_PLACE_DONE
        DO: @T_PLACE_OPEN
        FIRES: @E_OPEN_GRIPPER_DONE
    R_OPEN_GRIPPER_DONE:
        WHEN: @E_OPEN_GRIPPER_DONE
        DO: @T_OPEN_RETURN
        FIRES: @E_RETURN_HOME_DONE
    R_RETURN_HOME_DONE:
        WHEN: @E_RETURN_HOME_DONE
        DO: @T_RETURN_EXIT


        
